package ownews.dao;

import ownews.bean.Register;

/**
 *
 * @author Shreya Appia
 * provides interface for Register DAO functions
 */
public interface RegisterDao {
    public boolean addUser (Register register);
}
